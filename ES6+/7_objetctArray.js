/**
 * Object es un conjunto de propiedades (clave: valor)
 */
const jugador={nombre:"María", age:20, estado: true};
console.log(jugador);
/**
 * Array: conjunto de elementos (posicion)
 */
const datos=["Colombia", 20, true,[10,20], null];
console.log(datos);